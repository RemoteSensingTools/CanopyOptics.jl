# Legacy Source Archive

Files in this directory are not included by `CanopyOptics.jl`.

They are kept only as historical references while the canopy scattering API is
being consolidated. Do not add new production code here; put active canopy
Z-matrix code under `src/canopy_scattering/`.
