# Canopy Structure Source Map

This folder contains canopy-structure modifiers that affect interception and
propagation, not the per-element scattering matrix.

- `clumping.jl`: clumping-index models and `effective_G = Ω(μ) G` helpers.
